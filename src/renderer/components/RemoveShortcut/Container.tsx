import SettingsModal from '../Settings/Modal';
import RemoveShortcutForm from './Form';

function RemoveShortcutContainer() {
  return (
    <SettingsModal title="Remove Shortcut">
      <RemoveShortcutForm />
    </SettingsModal>
  );
}

export default RemoveShortcutContainer;
